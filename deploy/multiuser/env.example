# NCE v1.0 — legacy path beside deploy/multiuser/docker-compose.yml
#
# Canonical Compose defaults live in: deploy/compose.stack.env
# Preferred stack: repository-root docker-compose.yml
#
# If you still run from this directory:
#   cp ../compose.stack.env .env
#   docker compose up -d --build
